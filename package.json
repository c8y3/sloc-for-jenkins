{
  "name": "sloc-for-jenkins",
  "version": "0.0.2",
  "description": "Counts lines of JavaScript code and outputs the result into the format expected by the sloccount Jenkins plugin",
  "bin": {
    "sloc-for-jenkins": "bin/sloc-for-jenkins"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "linecount",
    "jenkins",
    "sloc",
    "sloccount"
  ],
  "author": "Charles Hymans",
  "license": "GPL-3.0",
  "dependencies": {
    "commander": "^2.8.1",
    "readdirp": "^1.3.0",
    "sloc": "^0.1.9"
  }
}
