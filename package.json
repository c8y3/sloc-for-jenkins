{
  "name": "sloc-for-jenkins",
  "version": "0.0.0",
  "description": "uses sloc to count lines of code and converts them into the format expected by the sloccount Jenkins plugin",
  "bin": {
    "sloc-for-jenkins": "sloc-for-jenkins"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "linecount",
    "jenkins",
    "sloc",
    "sloccount"
  ],
  "author": "Charles Hymans",
  "license": "GNUGPLv3",
  "dependencies": {
    "commander": "^2.8.1",
    "sloc": "~0.1.9"
  }
}
